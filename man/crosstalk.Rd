% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosstalk.R
\name{crosstalk}
\alias{crosstalk}
\title{Run Crosstalk Inference Pipeline}
\usage{
crosstalk(
  enrichment_df,
  expr,
  group_names = NULL,
  iter = 100,
  convert.ID = NULL,
  species = NULL,
  mapping_file = NULL,
  net = NULL,
  prefix = "res",
  ...
)
}
\arguments{
\item{enrichment_df}{A \code{data.frame} describing enriched terms and their member genes
(e.g., output from \code{\link[=enrich_path]{enrich_path()}} or a compatible table). The first column must
contain term/pathway identifiers and the second column must contain gene members
as a single character string separated by commas (\verb{,} ) or slashes (\code{/}), as expected
by \code{\link[=prepare_sets]{prepare_sets()}}.}

\item{expr}{A numeric matrix or \code{data.frame} of expression-like values with \strong{genes as rows}
and \strong{samples as columns}. Gene identifiers must be in \code{rownames(expr)}.}

\item{group_names}{Optional character vector of patterns used to define sample groups
by matching against \code{colnames(expr)} (e.g., \code{c("Normal", "Tumor")}). Passed to
\code{\link[=prepare_data]{prepare_data()}} for group-aware processing.}

\item{iter}{Integer. Number of permutations used to generate the null distribution
for the crosstalk statistic. Default is \code{100}.}

\item{convert.ID}{Optional. If not \code{NULL}, triggers gene ID conversion in
\code{\link[=prepare_data]{prepare_data()}} via \code{\link[=map_me]{map_me()}}.}

\item{species}{Optional character. Species identifier passed to \code{\link[=prepare_data]{prepare_data()}}
(and ultimately to \code{\link[=map_me]{map_me()}}) for gene ID mapping.}

\item{mapping_file}{Optional. Reserved for custom ID mapping files. Currently not
used inside \code{crosstalk()}.}

\item{net}{Optional edge-list \code{data.frame} defining a background interaction network.
If \code{NULL}, \code{\link[=retrieve_net]{retrieve_net()}} loads the default network bundled with the package.
The network must contain at least two columns representing edge endpoints; additional
columns (e.g., \code{mor}) are allowed.}

\item{prefix}{Character. Prefix used to name outputs generated by
\code{\link[=compute_crosstalk]{compute_crosstalk()}}. Default is \code{"res"}.}

\item{...}{Additional arguments (currently unused). Reserved for future extensions.}
}
\value{
A \code{data.frame} of pairwise gene-set interactions as returned by
\code{\link[=compute_crosstalk]{compute_crosstalk()}}. The output typically includes observed interaction degree,
MI-based scores (e.g., \code{mi_score}, \code{mi_norm}), overlap metrics (e.g., Jaccard index),
and permutation-derived p-values.
}
\description{
Integrates pathway enrichment results, expression data, and a background interaction
network to infer pathway–pathway (or TF–pathway) crosstalk using mutual information (MI)
and permutation testing.
}
\details{
The pipeline performs the following steps:
\enumerate{
\item Constructs gene sets from \code{enrichment_df} using \code{\link[=prepare_sets]{prepare_sets()}}.
\item Preprocesses expression data and computes per-gene z-scores using \code{\link[=prepare_data]{prepare_data()}}.
\item Loads or constructs a background interaction network via \code{\link[=retrieve_net]{retrieve_net()}}.
\item Computes mutual information (MI) for network edges using \code{\link[=compute_mi]{compute_mi()}}.
\item Estimates pathway crosstalk statistics and permutation-based p-values
using \code{\link[=compute_crosstalk]{compute_crosstalk()}}.
}

Progress messages and total runtime (in seconds) are printed during execution.
}
\examples{
\dontrun{
res <- crosstalk(enrichment_df, expr,
                 group_names = c("Normal", "Tumor"),
                 iter = 100)
head(res)
}
}
\seealso{
\code{\link[=prepare_sets]{prepare_sets()}}, \code{\link[=prepare_data]{prepare_data()}}, \code{\link[=retrieve_net]{retrieve_net()}},
\code{\link[=compute_mi]{compute_mi()}}, \code{\link[=compute_crosstalk]{compute_crosstalk()}}
}

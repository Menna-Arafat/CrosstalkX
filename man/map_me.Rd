% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_me.R
\name{map_me}
\alias{map_me}
\title{Map identifiers to gene symbols (and symbols to Ensembl gene IDs)}
\usage{
map_me(ID = NULL, species = "human", convert.ID = NULL)
}
\arguments{
\item{ID}{Character vector of identifiers to map (e.g., Ensembl gene IDs, UniProt IDs, Entrez IDs, or gene symbols).}

\item{species}{Character scalar indicating the organism. Supported values are:
\itemize{
\item \code{"human"} (Ensembl dataset: \code{hsapiens_gene_ensembl})
\item \code{"mmusculus"} (Ensembl dataset: \code{mmusculus_gene_ensembl})
}}

\item{convert.ID}{Character scalar specifying the conversion to perform. Supported values are:
\itemize{
\item \code{"ensembl.gene_to_symbol"}: Ensembl gene ID \eqn{\rightarrow} gene symbol
\item \code{"ensembl.protein_to_symbol"}: Ensembl peptide/protein ID \eqn{\rightarrow} gene symbol
\item \code{"uniprot_to_symbol"}: UniProt GN ID \eqn{\rightarrow} gene symbol (isoform suffixes like \code{-1} are removed)
\item \code{"uniprot_swiss_to_symbol"}: UniProt Swiss-Prot accession \eqn{\rightarrow} gene symbol
\item \code{"entrezid_to_symbol"}: Entrez gene ID \eqn{\rightarrow} gene symbol
\item \code{"symbol_to_ensembl"}: gene symbol \eqn{\rightarrow} Ensembl gene ID
}}
}
\value{
A character vector of mapped identifiers (same length as \code{ID}). Unmapped entries are \code{NA}.
}
\description{
\code{map_me()} maps a vector of identifiers to gene symbols (or maps gene symbols to Ensembl gene IDs)
using Ensembl resources. Depending on \code{species} and \code{convert.ID}, mapping is performed via
\pkg{biomaRt} (Ensembl BioMart). For human Ensembl gene IDs, an \pkg{EnsDb} backend may be used
if implemented in your code path.
}
\details{
Mapping is performed using Ensembl BioMart via \code{biomaRt::useMart()} and \code{biomaRt::getBM()}.
The returned vector is aligned to the input \code{ID} (same length and order).

For \code{"uniprot_to_symbol"}, isoform suffixes in the input (e.g., \code{P12345-1}) are removed prior to querying.

For \code{species = "human"}, gene symbols correspond to HGNC symbols; for \code{species = "mmusculus"},
symbols correspond to MGI symbols.

\strong{Important:} BioMart queries require an internet connection and can fail due to Ensembl downtime
or network restrictions. Consider caching results for reproducibility.
}
\examples{
\dontrun{
# Ensembl gene IDs -> symbols
ids <- c("ENSG00000141510", "ENSG00000155657")
map_me(ID = ids, convert.ID = "ensembl.gene_to_symbol", species = "human")

# UniProt Swiss-Prot -> symbols
uniprot_ids <- c("P04637", "P38398")
map_me(ID = uniprot_ids, convert.ID = "uniprot_swiss_to_symbol", species = "human")

# Symbols -> Ensembl gene IDs
map_me(ID = c("TP53", "EGFR"), convert.ID = "symbol_to_ensembl", species = "human")
}
}
\seealso{
\code{\link[biomaRt]{useMart}}, \code{\link[biomaRt]{getBM}}
}

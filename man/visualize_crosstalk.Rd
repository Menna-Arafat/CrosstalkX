% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize_crosstalk.R
\name{visualize_crosstalk}
\alias{visualize_crosstalk}
\title{Visualize term Cross-talk as a Network Graph}
\usage{
visualize_crosstalk(
  sig,
  named_labels = NULL,
  named_colors = NULL,
  top = 1,
  layout = "circle",
  prefix = "res"
)
}
\arguments{
\item{sig}{A \code{data.frame} of term–term interactions (e.g., output of
\code{\link[=compute_crosstalk]{compute_crosstalk()}} or \code{\link[=crosstalk]{crosstalk()}}). Must contain at least the columns
\code{Term1}, \code{Term2}, and \code{mi_norm}. Only these three columns are used for graph
construction.}

\item{named_labels}{Optional named character vector used to relabel node names via
\code{\link[dplyr:recode]{dplyr::recode()}}. Names correspond to original term IDs and values correspond to
the desired display labels.}

\item{named_colors}{Optional named character vector of colors (e.g., hex strings)
used to color nodes. Names should match original term IDs (before relabeling).
Nodes without a match (or when \code{named_colors = NULL}) are assigned a default color.}

\item{top}{Numeric in (0, 1]. Fraction of highest-degree nodes to retain for visualization.
The function keeps the top \code{ceiling(n_nodes * top)} nodes by degree. Default is \code{1}
(retain all nodes).}

\item{layout}{Character. Layout string passed to \code{\link[ggraph:ggraph]{ggraph::ggraph()}} (e.g., \code{"circle"},
\code{"fr"}, \code{"kk"}). Default is \code{"circle"}.}

\item{prefix}{Character. Prefix used for output filenames written to disk.}
}
\value{
A \pkg{ggplot2} object representing the network plot. The plot is also saved
to disk as a PNG. A node-degree table is saved as a CSV.
}
\description{
Create an undirected network visualization of inferred term–term cross-talk.
Nodes represent terms (e.g., pathways/TF programs) and edges represent interactions.
Node size is scaled by node degree, and edge width is scaled by the normalized
mutual-information score (\code{mi_norm}).
}
\details{
The function performs the following steps:
\enumerate{
\item Builds an undirected \pkg{igraph} object from \code{sig[, c("Term1","Term2","mi_norm")]}.
\item Computes node degree on the full graph and writes a degree table to
\code{"<prefix>_crosstalkx_terms_degree.csv"}.
\item Retains only the top fraction of nodes by degree as specified by \code{top}.
\item Assigns node colors using \code{named_colors} if provided; otherwise uses a default color.
\item Optionally relabels nodes using \code{named_labels}.
\item Converts to a \pkg{tidygraph} object, rescales node sizes and edge widths, and
renders the network using \pkg{ggraph}.
\item Saves a PNG to \code{"<prefix>_crossTalkX_plot_top<top>.png"}.
}
}
\examples{
\dontrun{
# sig is a data.frame with Term1, Term2, and mi_norm
p <- visualize_crosstalk(sig, top = 1, layout = "circle")
p

# optional relabeling and coloring
labs <- c("hsa04110" = "Cell cycle", "hsa04010" = "MAPK signaling")
cols <- c("hsa04110" = "#1b9e77", "hsa04010" = "#d95f02")
p2 <- visualize_crosstalk(sig, named_labels = labs, named_colors = cols, top = 0.5)
p2
}
}
\seealso{
\code{\link[=compute_crosstalk]{compute_crosstalk()}}, \code{\link[=crosstalk]{crosstalk()}}
}

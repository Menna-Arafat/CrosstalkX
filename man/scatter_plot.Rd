% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scatter_plot.R
\name{scatter_plot}
\alias{scatter_plot}
\title{Create a Scatter Plot Between Two Crosstalk Metrics With Correlation Annotation}
\usage{
scatter_plot(
  result,
  x = "Jaccard",
  y = "pval_deg_norm",
  log_transform_x = TRUE,
  log_transform_y = TRUE,
  model.fit = "spearman",
  model.visual = "loess",
  prefix = "res"
)
}
\arguments{
\item{result}{A data.frame containing crosstalk results (e.g., output of
\code{\link{crosstalk}} or \code{\link{compute_crosstalk}}). Must contain the columns
specified by \code{x} and \code{y}.}

\item{x}{Character. Column name to use for the x-axis. Default is \code{"Jaccard"}.}

\item{y}{Character. Column name to use for the y-axis. Default is \code{"pval_deg_norm"}.}

\item{log_transform_x}{Logical. If \code{TRUE}, transforms x-values as \code{log10(x + 0.001)}.
Default is \code{TRUE}.}

\item{log_transform_y}{Logical. If \code{TRUE}, transforms y-values as \code{-log10(y + 0.001)}.
Default is \code{TRUE}.}

\item{model.fit}{Character. Correlation method passed to \code{stats::cor.test()}.
Typical options include \code{"spearman"}, \code{"pearson"}, and \code{"kendall"}. Default is \code{"spearman"}.}

\item{model.visual}{Character. Smoother method passed to \code{ggplot2::geom_smooth()}.
Default is \code{"loess"}.}

\item{prefix}{name of the output}
}
\value{
A \pkg{ggplot2} object (the scatter plot). The plot is also saved to disk
as a PNG file.
}
\description{
Generates a scatter plot for two selected columns from a crosstalk results data frame,
optionally applies log transformations, overlays a smoothing curve, and annotates the
plot with a correlation coefficient and p-value. The plot is also saved as a PNG file.
}
\details{
The function creates two temporary columns \code{x_val} and \code{y_val} after optional
transformation. Correlation is computed using \code{stats::cor.test()} on these
transformed values. A PNG file is written to the working directory named
\code{"scatter_plot_<x>_vs_<y>.png"}.

Note: The annotation label currently reports "Spearman r" regardless of \code{model.fit}.
If you use \code{"pearson"} or \code{"kendall"}, consider updating the label accordingly.
}
\examples{
\dontrun{
# result is a data.frame with columns "Jaccard" and "pval_deg_norm"
p <- scatter_plot(result, x = "Jaccard", y = "pval_deg_norm",
                  model.fit = "spearman", model.visual = "loess")
p
}
}

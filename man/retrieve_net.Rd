% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retrieve_net.R
\name{retrieve_net}
\alias{retrieve_net}
\title{Prepare a Gene Interaction Network}
\usage{
retrieve_net(net = NULL)
}
\arguments{
\item{net}{A data frame representing an interaction edge list. The first two
columns are assumed to correspond to source and target nodes (e.g., genes or TFs).
Additional columns (e.g., mode of regulation) are allowed. If \code{NULL}, the
default network dataset \code{net} from \pkg{CrosstalkX} is used.}
}
\value{
A \code{data.frame} containing a cleaned interaction network with at least
the following columns:
\describe{
\item{source}{Source node (gene or transcription factor).}
\item{target}{Target node (gene).}
}
}
\description{
Prepares a gene–gene interaction edge list for downstream crosstalk analysis.
If no network is provided, a default interaction network bundled with
\pkg{CrosstalkX} is loaded.
}
\details{
This function performs the following steps:
\enumerate{
\item Loads the default interaction network from \pkg{CrosstalkX} if \code{net} is \code{NULL}.
\item Renames the first two columns to \code{source} and \code{target}.
\item Removes edges with missing or empty source/target entries.
\item Removes duplicated source–target pairs while preserving additional columns.
}
}
\examples{
\dontrun{
# Use the default CrosstalkX interaction network
net1 <- retrieve_net()

# Provide a custom interaction network
net2 <- retrieve_net(custom_net)
}
}
